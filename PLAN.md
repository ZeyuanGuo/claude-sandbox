# 受控开发机实施计划

## 目标

在共享 GPU 服务器上，为一个用户长期运行一套受控开发机。目标软件可以正常读写登记的项目、运行命令和使用全部 GPU，但不能直接接触宿主网络、局域网或绕过受控网关访问公网。

当前支持的出站路径必须留下可核对的 DNS、PCAP、进程调用或明文证据。指定检测对象的普通 HTTP/HTTPS 放行连接还必须能够关联到发起进程、实际数据包、请求明文和响应元数据；需要响应正文时使用专项捕获。遇到未覆盖的内核接口、协议或加密方式时继续阻断并单独验证。

第一套检测对象是 Claude Code。系统本身不能依赖 Claude Code，以后应能用同一流程检查其他软件。

## 已经确定的边界

- 每个用户在一台服务器上只有一套受控开发机。多个项目通过增加挂载加入，不为每个项目创建新沙箱。
- 只有一名实际操作者。沙箱日常 shell 使用与宿主同名、同 UID/GID 的普通账号，并可在容器内使用 `sudo`；宿主 Docker、网络和审计管理仍只能从外层提权执行。容器不挂 Docker socket、宿主审计目录或宿主网络命名空间。
- 当前不把内核漏洞、驱动漏洞和容器逃逸纳入保证范围。
- 登记的项目目录可以直接读写。每台服务器自行选择项目，通用代码不写死项目名称。
- 全部 GPU 可见并允许使用，不修改 GPU 的独占模式，也不自动停止其他用户的任务。
- 任何涉及其他用户进程的停止、暂停或修改，都必须先获得本次明确确认。
- 不开放沙箱 Web 端口，不建设 VNC 或浏览器转发。当前主要入口是 shell 和 TUI。
- 网络从全断开始。新软件和新认证方式先使用严格策略，未知 Web 请求在连接真实上游前暂停；解释核心流量并完成真实任务后，可以单独生成日常策略。公开仓库提供公共 Web 日常策略：普通公网 HTTP/HTTPS 默认放行并记录，其他直接 IP、外部 DNS、特殊主机名、已知加密 DNS、非 Web 端口和默认 UDP/QUIC 仍保持阻断。历史私有 API 端点不随发布策略提供。
- 普通 TLS 默认解密。解密失败不能自动改成密文直通。
- 上游 VPN 或代理只负责把已批准流量送到目标出口，不负责决定放行什么。
- 策略、镜像、控制代码和文档可以私有发布；项目、开发环境、凭据和普通日志只留在各服务器本地。

## 需要如实说明的限制

容器隐藏的是宿主网络和未挂载的文件，不是完整的虚拟硬件。目标软件仍可能看到真实 CPU 架构、内核系列、GPU 型号和数量、容器特征，以及所有已挂载项目中的内容。暴露全部 GPU 与“完全伪造一台陌生机器”不能同时成立。

地域一致性只承诺下面这些可控制的项目：

- 出口由外层上游提供，并在本机配置中记录期望的出口身份；
- DNS 查询经同一上游路径，不使用宿主默认 DNS；
- 容器时区由主机私有配置指定，公开默认是 `UTC`；同时使用 `en_US.UTF-8`、常见英文字体和一致的英文终端环境。SSH/TUI 的键盘映射由用户终端决定，容器不伪造不存在的图形桌面设置；
- 使用中性的主机名、用户配置和干净 Home，不复制宿主 shell 历史或个人配置。

系统不能仅凭一个域名判断请求正文是否恶意。日常策略允许普通公网 HTTP/HTTPS 后，目标软件可以向新的公网网站发送数据；当前系统依靠完整明文记录和事后分析发现问题，还没有独立的首次域名提醒服务。因此，指定软件进入日常策略前至少要完成核心流量解释、真实任务和基本门禁；凭据挂载仍需单独评估。

## 部署前提

每台服务器都要独立检查，不能继承其他机器的现场结论：

| 项目 | 要求 | 对部署的影响 |
|---|---|---|
| 系统 | Ubuntu 24.04 或单独验证的兼容系统，cgroup v2 和 AppArmor 可用 | 才能使用当前容器 cgroup、nftables 和 eBPF 路径 |
| Docker | Docker Engine 与 Compose 可用；操作通过宿主 root 完成 | 不把普通用户加入 `docker` 组 |
| GPU | 主机实际 GPU，计算模式保持原值 | 通过 CDI `nvidia.com/gpu=all` 提供给目标容器，不修改独占模式 |
| NVIDIA Container Toolkit | 由每台服务器单独安装和验收 | 迁移时按驱动和 CDI 状态重新确认 |
| 网络和审计工具 | nftables、tcpdump、bpftool、bpftrace 可用 | 用宿主 `nsenter + tcpdump` 抓网络命名空间，用目标 cgroup 的 bpftrace 记录连接和执行 |
| 存储 | 根分区和审计分区满足主机配置中的保留线和运行预算 | 不在空间不足时拉取镜像或启动审计运行 |
| 版本管理 | 使用独立 Git 仓库 | 只发布本项目，不把整个用户 Home 当成项目仓库 |

根分区默认保留线为 `max(50 GiB, 容量的 5%)`；镜像操作前的可用空间还必须覆盖新增镜像独占层、构建峰值临时量和容器可写层预算。构建和审计需要持续检查，不能运行无范围的清理。

空间不足时，先对项目状态目录、审计目录、构建缓存和登记的 Docker 服务做定向只读审计，生成包含精确路径或 Docker ID、归属、大小、用途、活动引用、保留期和恢复方式的清理计划。只有本项目明确拥有、已经超过保留期或可重新生成的内容，才能在操作者确认清单后清理；执行前必须重新检查对象未变化，变化即拒绝。不得自动运行 `docker system prune`，不得删除共享镜像层、共享 Docker 数据或其他用户文件。迁移共享 Docker 数据目录、删除共享对象或启动第二个 Docker daemon 都必须另行确认。完整规则见 [存储管理](docs/storage.md)。

## 目标结构

```text
受控开发机容器
  |-- 普通 DNS -> 受控 DNS -> 父代理 -> 固定 DoH 服务
  |                         \-> 返回公网地址并签发名称-IP租约
  |
  |-- 普通 HTTP/HTTPS -> nftables 透明重定向 -> mitmproxy
                                               |-- 核对原始目的 IP、DNS 租约和 Host/SNI
                                               |-- 执行 strict/daily 内容策略并保存明文
                                               \-> 父代理连接已经解析的公网 IP
```

目标容器没有代理变量，也不能连接宿主代理、Docker socket、审计目录和网关管理接口。只有受控 DNS 和网关加入无公网路由的内部 `upstream_net`，并通过固定网桥宿主地址访问 `11440`。宿主 nftables 只放行这两个固定来源，并阻断该网桥访问其他宿主端口。父代理的具体实现和 VPN/IP 集成由本机负责，不进入通用仓库。

当前使用 `Docker internal network + 受控 DoH + DNS-IP租约 + nftables透明重定向 + mitmproxy transparent + 宿主父 HTTP 代理`。DNS 和 Web 都经父代理出站；Web 上游按受控 DNS 返回的 IP 连接，原域名继续用于 SNI 和证书校验。严格策略暂停未知 Web；日常策略允许普通公网 Web，但继续阻断直接 IP、特殊主机名、异常端口和不可解密连接。

API-key 的完整 Claude 功能基线来自迁移前的显式代理运行；透明路径已经通过封闭门禁、受控 DNS、DNS 租约、多运行时 HTTPS、明文和探针失效闭锁。还没有在透明条件下重跑同一套 Claude 功能矩阵，因此两类证据必须分开，不能把基础网络通过写成账号模式或完整功能通过。

完整抓包至少覆盖目标侧 veth、目标网络命名空间的 loopback，以及网关或 TUN 出口侧。只抓目标 veth 会漏掉容器内 `127.0.0.1` 的连接尝试和代理终止 TLS 后的真实上游连接。连接归属覆盖整个容器 cgroup，不按单个 PID 过滤。

## 配置、状态和发布内容

通用仓库只保存可复现逻辑：

```text
bin/                 用户命令
controller/          生命周期、策略切换和故障处理
images/              受控开发机和网关镜像
gateway/             DNS、nftables、TLS 解密和上游接口
policies/            不可修改的策略版本
targets/             Claude Code 等检测预设
tests/               阻断、捕获、故障和性能验收
docs/                设计、操作和研究依据
examples/            不含秘密的主机配置示例
```

每台服务器自己保存：

```text
~/.config/controlled-dev-machine/host.yaml
~/.local/share/controlled-dev-machine/home/
~/.local/state/controlled-dev-machine/audit/
~/.local/state/controlled-dev-machine/runtime/
```

实际路径可以在主机配置中修改。配置中的 `~` 和相对 Home 路径必须相对目标用户的 Home 解析，不能使用启动 `sandboxctl` 的 root 账号 Home。项目路径尽量使用 Home 相对形式记录，并在容器内映射到相同绝对路径，例如宿主和容器都使用 `/home/alice/project`。

`host.yaml` 至少声明：目标用户及其 Home、UID/GID、持久 Home、项目挂载、本地开发环境、GPU 暴露、审计目录、上游配置位置、期望出口、各数据类型硬上限、分区保留线和默认策略。上游凭据、CA 私钥和账号令牌只引用本地只读文件，不能直接写入仓库配置。

控制层只维护三类核心数据：

1. **主机配置**：描述这一台服务器有什么路径、GPU、存储和上游，不随版本发布。
2. **策略快照**：描述哪些域名、协议和端口获准，以及规则依据、父版本和到期时间。快照创建后不可修改。
3. **运行清单**：描述一次测试实际使用的软件哈希、命令、配置、策略、出口、探针和结果文件。

每份策略快照明确声明 Web 默认动作，以及规则编号、用途、动作、精确域名或子域范围、HTTP 方法、路径范围、内容类型、请求大小上限、传输协议、端口、TLS 身份要求、明文要求、证据和确认时间。严格策略的 Web 默认动作是暂停审核，而不是直接连接上游；已批准规则才允许转发。日常策略的 Web 默认动作可以改为允许符合边界的公网 HTTP/HTTPS，但只有受限观察完成并作出独立决定后才能创建。明确拒绝、非 Web 例外和可信业务请求仍使用具体规则。严格、日常、维护和观察是四份完整策略快照，不是一组会互相组合的布尔开关。

待审核请求使用固定状态机：`pending -> approved_once -> authorized -> completed | upstream_error`，或进入 `blocked | expired | client_disconnected`。单次批准绑定请求正文哈希、目标、过期时间和当前策略版本，不能批准另一条相似请求；需要长期放行时创建新的不可修改策略快照。目标软件不能读取或修改审核队列和批准记录。

所有运行时状态由控制器生成，目标容器没有写权限。审计记录只引用策略快照和运行清单的内容哈希，不从当前配置反推过去发生了什么。

## 日常命令

当前已实现的用户入口：

```text
sudo sandboxctl init
sudo sandboxctl start
sudo sandboxctl stop
sudo sandboxctl status
sudo sandboxctl shell
sudo sandboxctl root-shell
sudo sandboxctl storage plan
sudo sandboxctl audit status
sudo sandboxctl doctor
sudo sandboxctl verify-closed
```

自动化的 `policy set`、存储清理执行、上游切换和 `target test claude-code` 仍属于后续实现，不把规划入口当成当前可用命令。策略切换目前使用 `stop -> init --policy -> build -> start -> verify-closed`。Claude Code 的历史 API-key 基线已完成严格逐请求发现，并在本机日常策略下通过自动与人工真实任务；公开仓库只提供脱敏后的通用策略。

`root-shell` 内部使用 `docker exec --user root`。宿主密码明文不进入脚本、配置、镜像或日志；`init` 会读取宿主密码哈希并通过 root-only 运行文件配置容器内 `sudo`。命令只能管理本用户登记的容器、网络和 cgroup；发现目标落到其他范围时必须拒绝执行。

## 实施阶段

### 阶段 0：冻结设计和建立独立仓库

工作：

- 以 `readme.md` 作为当前入口，本计划、专题文档和 ADR 保存设计与公开证据；原始沟通和主机现场记录不进入发布仓库。
- 固定当前 `Docker internal network + 受控 DoH + DNS-IP租约 + nftables透明重定向 + mitmproxy transparent + 宿主父 HTTP 代理` 的版本和现场验收边界；TUN/L3 适配器保留为以后可替换的上游方案。
- 建立独立私有 Git 仓库和基本目录，不连接或发布到远端，直到用户提供私有仓库位置。
- 定义 `host.yaml`、策略文件、运行记录和审计索引的结构。
- 记录当前主机的只读基线，不修改 Docker、GPU、网络或其他进程。
- 建立存储预检：分别检查根分区、审计目录和状态目录的剩余空间，并根据镜像、日志保留期和运行预算计算启动所需空间。
- 由宿主 root 只读记录登记 Docker 服务的 data root、存储驱动、日志驱动、镜像、容器、构建缓存和磁盘占用；发现其他 Docker socket 或 daemon 时只记录存在，不探查、不管理。

产物：总计划、配置示例、架构图、技术选型说明、存储预算和独立 Git 历史。

通过条件：所有确认过的决定都能在正式文档中找到；没有密码、令牌、真实上游配置和主机私有地址进入仓库。

### 阶段 1：实现安全的主机控制入口

工作：

- 实现 `sandboxctl doctor`、配置校验、计划预览和资源归属检查。
- 每次 root 操作先解析准确对象，再限制到本用户的容器、网络接口、nftables 表和状态目录。
- 不执行全局 `iptables -F`、`nft flush ruleset`、`docker system prune`、GPU 模式修改或不带范围的进程终止。
- 空间不足时只生成定向清理计划。执行命令必须引用这份不可修改的计划，并再次检查路径归属；共享 Docker 对象和其他用户内容只能报告，不能自动删除。
- 所有创建动作可重复执行；部分失败时回到网络全断状态。

产物：CLI 骨架、配置解析、dry-run 输出、操作审计。

通过条件：错误配置不会产生半套网络；工具无法对未登记容器、其他用户进程或共享 Docker 对象执行修改。

### 阶段 2：建立可日常使用的容器

工作：

- 使用固定 digest 的 Ubuntu 24.04 基础镜像，创建与宿主 UID/GID 一致的普通用户，并按本机密码哈希配置容器内 `sudo`。
- 按主机私有配置设置时区，配置英文 locale、常见英文字体、中性主机名、bash、tmux、编辑器、Git、Python、常用诊断和构建工具。
- 使用独立持久 Home，不复制整个宿主 Home、shell 历史或整棵 `.claude`。`config init` 生成受控 shell/profile 副本，并按后续明确选择默认挂载宿主 Git/SSH 等常用开发配置。
- 基础设施容器丢弃全部 capability 并启用 `no-new-privileges`；目标容器为支持 setuid `sudo` 保留 Docker 默认 capability 集，但不使用 `--privileged`，不获得 `NET_ADMIN`、Docker socket或宿主命名空间。
- 不发布端口，不挂载 Docker socket、宿主 `/proc`、`/sys` 或 `/run`。
- 使用 NVIDIA Container Toolkit CDI 把全部 GPU 提供给目标容器，设置 `compute,utility` 能力；不修改 GPU 独占模式，不自动停止其他任务。

产物：基础镜像、固定软件清单、持久 Home、shell 与 root 进入文档。

通过条件：普通用户可在容器内使用 `sudo`，但不能改变宿主网络或取得 Docker/宿主命名空间；root 可从宿主进入；重建容器后 Home 保留；全部 GPU 可计算且没有修改独占模式。

### 阶段 3：先完成物理网络全断

当前实现：

- 目标只加入 Docker `internal` 的 `target_net`，没有默认公网路由，也没有 `NET_ADMIN`。
- 目标的系统 DNS 只到受控 DNS；受控 DNS 经父代理使用固定 DoH 端点解析，不使用宿主默认 DNS。
- 目标普通访问 TCP/80、443 时由 nftables 透明送到网关；写死公网 IP、TCP/22、外部 DNS 和 UDP 没有直通路径。
- 宿主用目标网络命名空间 PCAP 和目标 cgroup eBPF 同时记录实际包、进程执行、连接参数和返回值。调用在发包前返回 `ENETUNREACH` 时，以系统调用记录作为证据。
- 目标、DNS 和网关的短期 nftables 放行项只由宿主 watchdog 续期；必要探针退出后自动过期。

产物：隔离目标网络、受控 DNS、目标/DNS/网关 PCAP、cgroup 连接记录和阻断测试。

当前通过条件已经满足：直接公网、TCP/22、UDP、外部 DNS 和代理绕过不能到达真实目标，并能定位到测试进程。IPv6、DoT、广播/多播和完整私网 canary 矩阵仍需扩展，不写成已经全部验收。

### 阶段 4：接入策略网关和上游适配器

宿主配置接入本机父 HTTP 代理 `127.0.0.1:11440`，并在主机私有配置中登记实际出口范围。目标不加入上游网络；只有受控 DNS 和网关通过内部网桥的宿主地址访问该端口。

已完成：

- mitmproxy transparent 使用 fail-closed addon，在上游调度前执行 strict/daily 完整策略。
- 受控 DNS 经父代理向固定 DoH 端点查询，拒绝非公网解析结果，并为名称和 IP 签发短租约；网关核对客户端原始目的 IP 与租约。
- 严格策略暂停未知 Web 并把一次性批准绑定到正文哈希；日常策略允许普通公网域名的 HTTP/80 和 HTTPS/443，同时保留明确允许和拒绝规则。
- `CONNECT` 只接受规范主机名和 443；旧式数字 IPv4、原始 TCP、请求 Upgrade、TLS 内应用层 `CONNECT` 和上游 `101` 都被阻断。发布策略默认阻断直接 IP；私有模型端点规则只存在于部署者本机，不进入发布仓库。
- 普通 TLS 由 mitmproxy 终止；网关保存请求正文、双向头、响应状态和传输结果，响应正文直接流式转发。目标只读持有 CA 公钥，网关保持上游证书校验和 lazy connection strategy。
- 策略摘要或请求 hook 故障时网关本地失败；网关健康检查定期实写审核目录，发现故障后锁存并阻断后续请求。日常请求本身不等待每次磁盘 `fsync`。
- 网关对非 canary 请求强制通过本机父代理连接已解析的目标 IP，同时保留业务域名用于 TLS SNI；不让父代理重新解析业务域名，也不回退为宿主直连。
- 策略、运行 generation 和网关镜像绑定摘要，测试策略、旧镜像或摘要不符时拒绝启动。

仍待完成：

- watchdog 当前验证进程身份和存活，不读取 tcpdump 内核丢包计数、eBPF 丢事件数或磁盘剩余空间；这些异常仍依靠启动预检和使用后审查。
- TUN/L3 或其他父代理实现可以以后替换，但必须保持同一 `11440` 接口和无直连回退，并在每台服务器重跑验收。

产物：mitmproxy fail-closed addon、CA 管理、父代理接口、可回滚策略 generation 和运行健康检查。

当前通过条件：严格审核和日常公共 Web 都可用；DNS 与 Web 经过同一父代理；直接 IP、特殊主机名、外部 DNS、非 Web `CONNECT`、原始 TCP、协议升级和 CA/策略故障保持阻断；没有宿主直连回退。受控 DNS、目的 IP 租约和审计租约均已实测。

### 阶段 5：完成审计和故障闭锁

工作：

- 使用宿主 `nsenter + tcpdump` 在目标和网关网络命名空间保存独立 PCAP；用目标 cgroup 范围的 bpftrace 记录进程执行、连接参数和返回值。tcpdump 通过宿主 root 预打开的 `0600` 文件描述符写入，目标容器不能改写证据。
- 对实际发现的 OpenSSL 文件、Go ELF 或其他实现逐个启用并验证 eBPF；不使用无 cgroup 限制的全主机捕获。若工具只支持精确 cgroup ID，就跟踪所有实际子 cgroup，不能把父 cgroup 当作自动覆盖整棵子树。
- 代理保存普通 TLS 请求明文和响应元数据；响应正文专项捕获、eBPF、会话密钥和应用 hook 只作为交叉验证或特殊路径补充。
- 在 connect/sendmsg 时记录 socket、五元组和进程，再与实际出站包关联；包事件本身不能可靠提供当前进程时，不用猜测 PID。
- 为每条连接生成统一编号，关联运行编号、策略版本、进程、可执行文件哈希、DNS、代理 flow ID、目标侧 PCAP、出口侧 PCAP 和明文。
- 完整 PCAP 与明文默认保留 72 小时，结构化记录保留 30 天。PCAP 的 100 GiB 硬上限是所有运行和所有抓包点的合计；明文初始硬上限为 50 GiB，结构化记录初始硬上限为 10 GiB。审计分区默认保留线为 `max(200 GiB, 容量的 10%)`。达到任一硬上限或保留线时停止联网，不能静默提前删除未到期证据。写盘失败、丢包、丢事件或截断时先断网，再暂停目标容器、报警并保留现场。
- 原始明文目录 root-only；普通报告隐藏令牌、Cookie、OAuth 代码和认证头。

产物：审计目录、统一事件格式、轮转规则、脱敏报告和故障监控。

通过条件：目标范围外的其他用户流量不会进入记录；任何必需捕获组件失效时，目标外网先关闭，不能继续密文通信。

### 阶段 6：使用受控 canary 验收基础能力

工作：

- 建立只供测试的 DNS、HTTP、TLS、UDP 和模拟私网接收端。
- 上传和下载带唯一编号的已知内容，覆盖超过 64 KiB、并发、长连接、TLS 1.2/1.3、HTTP/1.1/2、会话恢复和子进程。
- 对比原始内容与重建明文的长度、顺序和 SHA-256。
- 主动停止 DNS、网关、代理、探针和上游，制造磁盘不可写、错误 CA、事件丢失和进程替换。

产物：可重复的集成测试、每条用例的证据包和失败报告。

当前进度：`verify-closed` 已覆盖无代理环境、经父代理的普通公网 DNS、异常 DNS 查询在 DoH 前阻断、DNS 租约不匹配、直接 IP、TCP/22、普通 UDP/443、外部 DNS、非 Web 原始 TCP、请求 Upgrade、应用层 `CONNECT` 和上游 `101`，以及 HTTP/HTTPS 逐请求批准、拒绝、客户端断开、策略摘要故障和审核目录不可写。它还从目标容器核对固定公网查询落在本机 `expected_exit_cidr` 内，并保存网关到父代理的非零 PCAP。目标、DNS 和网关 PCAP、目标 cgroup eBPF 及代理请求明文均在运行。另已主动停止目标 PCAP，确认短租约过期后 DNS 和 Web 都关闭。超过 64 KiB、并发、长连接、TLS 版本差异、磁盘写满和完整丢事件统计仍未完成；当前业务运行的 HTTP/2 和停止时 PCAP 零内核报告丢包已有样本证据。

通过条件：开始 Claude Code 的有监督功能发现前，必须先通过默认全断、逐请求暂停、批准前不上游、策略与审核存储故障关闭这些基本门禁。把受限环境写成完整验收或开始日常观察前，还必须通过指定流量完整捕获矩阵。当前只满足前者。

### 阶段 7：逐步放行并检查 Claude Code 全部功能

API-key 基线已完成独立简答、连续会话、自动和人工交互开发、文件读写、测试、本地 Git、WebSearch、WebFetch、curl、Git HTTPS、子 agent、会话恢复、脱敏会话迁移、项目 Skill、MCP 工具/资源/prompt 和 hook。Claude Code 会发送沙箱内核、平台、架构、项目路径、Git 摘要、设备/会话标识、客户端版本和工具内容；已测人工任务没有发送实际时区、CPU/GPU、内存、MAC、内网 IP、代理或 CA 路径。fixture 证明扩展机制可用，但不自动信任其他扩展。原始 TCP、协议升级、旧式 IP 和特殊域名顶点旁路已经修复并复测。当前结论是被测 API-key 基线可日常使用，不是整个平台最终安全验收。公开边界见 `docs/validation.md`。

工作：

- 固定 Claude Code 版本、官方签名、二进制哈希、模型 ID、配置和费用上限；二进制由 root 所有并在每次运行前校验。
- API-key 与账号登录使用独立、可区分的认证状态和运行记录，不能混为一个结果。
- 从未登录冷启动开始。未知请求先在代理中暂停，检查域名、方法、路径、头字段类型和正文；只有必要且内容合理的请求才单次放行或形成窄规则。
- 由用户在自己的浏览器完成登录，把一次性代码输入沙箱终端；不开放沙箱浏览器或 Web 端口。
- 登录后依次启用三次独立简答、至少五轮连续会话、文件读取、修复小错误、本地测试、本地 Git、WebSearch、受控 WebFetch、子 agent、MCP、hook、Skill 和空闲观察。宿主未启用的插件、LSP、Chrome 或遥测只在用户准备实际使用时加入。
- 测试时保持正常用户可见的完整工具集，使用唯一运行编号，并对 API 流式请求、响应和本地输出做完整比对。
- 透明 MITM 的网络、无透传和普通 TLS 明文验收已经完成。下一步在透明条件下重跑账号登录和完整功能矩阵；需要判断检测是否改变行为时，再用同一任务、版本和出口与历史显式基线做对比，不能混用不同任务下的结果。

产物：Claude Code 固定版本基线、逐请求审核记录、功能与请求规则对应表、明文完整性结果、差异分析和待查项。

通过条件：每条必要连接用途明确、内容完整、故障闭锁有效、空闲无不明流量、不同检测条件之间没有未解释的稳定差异。

### 阶段 8：在受限策略下持续观察

原计划要求在逐请求策略中先观察 1 至 2 个活跃日。API-key 阶段在核心功能、真实任务和基本门禁通过后，操作者明确接受先进入“公共 Web 默认放行、非 Web 保持阻断、完整记录后分析”的日常方式。因此该本机基线改为在日常策略中继续观察；公开发布不携带私有端点规则，账号登录和新增联网组件仍从严格策略开始。

工作：

- Claude Code 核心功能可用后，继续使用同一版本和审计路径进行实际工作；时间经过本身不代表通过。
- 观察覆盖正常任务、长会话、搜索、PyPI、Git HTTPS、登录刷新、重试、插件、MCP、hook、技能、项目切换和空闲时段；持续审查正文、首次请求类型、进程变化、请求量和目标变化。
- 日常策略中的新公网 Web 请求先记录后分析；出现与任务无关的数据、异常编码或加密正文、不合理上传、探测行为、规则规避或延迟启动通信时，立即回到严格策略。

产物：日常观察审计、所有异常及解释，以及是否需要回退严格策略的结论。

通过条件：观察期内没有无法解释的内容、连接、进程或行为变化；基本门禁和明文记录持续有效。

### 阶段 9：决定是否生成日常策略

系统不能按时间自动切换。操作者根据已覆盖功能、实际流量和剩余风险单独决定。公共 Web 日常路径对应 `policies/daily/0001-public-web.yaml`；账号登录使用 `policies/strict/0002-claude-account.yaml`，不会自动继承私有 API-key 端点规则。

如果决定创建：

- 日常策略默认允许公网域名的 HTTP/HTTPS，只开放 TCP 80/443。当前实现保存请求正文、双向头字段、响应状态和传输结果，但尚没有独立的“首次域名提醒”服务；已确认恶意或无必要的域名进入拒绝规则。
- 直接 IP、危险地址、外部 DNS、DoT、已知 DoH、UDP/QUIC、非 Web 协议、TLS 身份不一致、解密失败和审计失败继续阻断。SSH、特殊下载端口和其他协议只能单独增加规则。
- 新域名上的自建 DoH 和应用层再次加密可能表现为普通 HTTPS，不能保证在发送前识别。发现无法解释的加密正文或通信模式后，立即加入待查并重新评估该目标的日常资格。
- 已验证的 Claude Code、搜索、PyPI 和 Git 业务域名保留用途分类，便于报告和回归测试；它们不再是日常 Web 能否访问的唯一依据。
- 每次更新记录差异、理由、证据、操作者、时间和测试结果；不在原文件上覆盖旧版本。
- 当前切换使用 `stop -> init --policy -> build -> start -> verify-closed`；初始化通过原子 generation 切换加载完整策略，失败不启用新 generation。公开发布从脱敏策略谱系开始；私有端点需要部署者在本机新增策略并重新验收。
- `maintenance` 和 `observe` 必须有 TTL，到期自动回到严格或日常策略。
- 严格策略中的未知 Web 请求继续暂停审核；日常策略中的普通公网 Web 域名放行并记录。私网、直接 IP、外部 DNS 和网关绕过保持阻断。DNS、网关、目标 PCAP、DNS PCAP、网关 PCAP 或目标 eBPF 进程退出后，watchdog 停止续期，网络放行项自动过期。

产物：明确的保持受限或进入日常决定；若进入日常，则产出首个日常策略、策略变更模板、回滚测试和简洁状态命令。

通过条件：若保持受限，现有策略继续稳定工作；若进入日常，问答、搜索、包安装、Git HTTPS 和新的公网 Web 域名可用且完整记录，非 Web 未知通信和所有旁路仍被阻断，并能恢复到上一份受限策略且没有混合状态。

### 阶段 10：接入真实项目和本地开发环境

工作：

- 基础网络和 Claude Code 核心检查通过后，再把正式项目按原绝对路径直接读写挂载。
- 初始化时列出实际挂载内容和风险。敏感子目录按主机配置遮蔽，不自动猜测项目秘密。
- 文本 profile 在 `init` 时生成只读受控副本并记录摘要；宿主内容变化后必须重新初始化。Skills、agents 和大型本地环境按主机声明同路径挂载，不复制进镜像或发布包。
- 每台服务器声明自己的默认环境，并检查实际 Python 路径、关键 import、CUDA 和 GPU 张量计算；通用逻辑不写死环境名，也不替服务器决定项目依赖。
- `config init` 检测常用开发配置：现有 `~/.config/git` 默认同路径读写挂载，现有 `~/.ssh` 默认只读挂载；操作者在初始化前删除不愿交给目标软件的条目。不会挂载整棵宿主 `~/.claude`。
- 运行项目提供的 smoke 命令和简单 GPU、CPU、文件系统性能对比。命令和阈值由各服务器配置，不把项目差异当成平台错误。

产物：本机挂载清单、可选环境接入、项目 smoke 结果和容器性能记录。

通过条件：容器内外登记项目路径一致；生成 profile 摘要可追溯；普通开发、tmux、Python 和 GPU 工作正常；没有读取未登记的宿主目录；正式项目变更可由宿主 Git 检查。

### 阶段 11：私有发布和多服务器复现

工作：

- 将控制仓库发布到私有 Git，固定 commit；目标和网关镜像使用构建输入摘要标签并记录 Docker 内容摘要，不使用 `latest` 或共享 `:dev`。私有 GHCR 可作为后续分发优化，不是新机从固定源码构建的前置条件。
- 发布包记录工具版本、策略版本、升级步骤、回滚步骤和验收命令。
- 新服务器只填写主机配置，拉取固定版本，运行 `doctor`、默认全断、canary 捕获、GPU smoke 和策略回滚。
- 不复制项目、环境、checkpoint、凭据、CA 私钥和普通日志。策略可同步，主机路径和上游秘密保持本地。

产物：首个固定发布、部署手册和 3 至 10 台服务器可重复的验收流程。

通过条件：另一台服务器只修改主机配置即可通过基础验收；缺少关键能力时明确停止，不降级为直连或无审计运行。

## 全程停止条件

出现以下任一情况时，不继续扩大权限或网络策略：

- 目标容器存在宿主直连、IPv6 泄漏或未受控 DNS 路径；
- 代理、PCAP、进程归属或必要明文捕获存在缺口；
- 日志写入失败、磁盘接近保留线、发生丢包、丢事件或截断；
- 需要修改共享 Docker 配置、清理共享存储或管理其他用户进程，但尚未取得明确确认；
- 目标软件、联网子程序、加密库、配置或版本发生未登记变化；
- 检测条件改变后出现无法解释的稳定行为差异；
- 上游出口身份或 DNS 路径与本机声明不一致。

停止后保留容器、规则版本、进程树、PCAP、明文、日志和文件哈希。先分析原因，再决定修复、增加针对性 hook、回滚策略或放弃放行。

## Claude Code 配置测试规则

Claude Code 采用分基线测试。API-key 核心程序和本地扩展 fixture 已完成严格发现并进入本机自己的日常策略；公开仓库只保留通用公共 Web 策略。账号登录、第三方插件/MCP/hook、软件升级或新的联网组件属于新的联网基线，开始时回到严格策略，分批复测后再决定是否生成下一份日常策略。
