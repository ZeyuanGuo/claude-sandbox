# 受控开发机

本项目让用户持续使用一个不能完全信任的软件，同时控制和审计它的网络访问。

## 核心对象

**受控开发机**:
每个用户在一台服务器上使用的一套长期沙箱。它的使用体验接近虚拟机，但安全边界仍是容器。
不要把它叫作“项目沙箱”或“虚拟机”。

**目标软件**:
可能探测环境、绕过网络限制，并执行任意普通用户命令的软件。当前不要求抵抗内核、驱动或容器逃逸漏洞。

**检测对象**:
已经固定版本和运行方式、需要完整分析网络行为的软件。Claude Code 是第一套预设检测对象，但系统不能依赖它的内部实现。

**项目挂载**:
加入受控开发机并允许目标软件直接读写的宿主目录。一个用户只有一套受控开发机，新增项目只增加挂载。

**本地开发环境**:
由每台服务器自行提供的开发环境。平台不检查其包版本，环境名和路径只存在于主机配置中。

**软件自身凭据**:
目标软件自己的账号、令牌或登录状态。它们由目标软件正常使用，不需要平台额外保护。

**宿主系统凭据**:
宿主上的 SSH 私钥、Git 令牌、云服务密钥和其他账号凭据。它们可能访问沙箱以外的资源，不能与软件自身凭据混为一类。

## 网络

**放行规则**:
允许一次出站通信所必须满足的条件，包括当前策略、目标、协议、端口和 TLS 握手中的域名。严格策略暂停未知 Web；日常策略允许普通公网 Web。发布策略默认阻断直接 IP；部署者如需私有直连端点，必须在本机新增精确规则并重新验收。

**严格策略**:
普通公网域名可以解析；已批准请求正常转发，未知 HTTP/HTTPS 请求在解密后、连接真实上游前暂停审核，其他未知通信阻断。它用于初始分析、功能扩展和后续受限观察。

**日常策略**:
操作者在核心流量、真实任务和基本门禁通过后单独创建。它默认允许经过解密代理并保存 flow 的公网 HTTP/HTTPS；当前没有首次域名提示服务。直接 IP、特殊主机名、外部 DNS、UDP/QUIC、非 Web 协议和旁路继续阻断。公开仓库提供的是通用公共 Web 日常策略；具体认证方式、版本和新增联网组件仍要单独建立基线。历史私有 API 端点不属于公开策略。

**维护策略**:
计划在目标软件停止后，临时用于安装软件、拉取代码或下载模型。当前控制器尚未允许部署这一模式。

**观察策略**:
计划用于临时分析未知公网通信，只能使用允许泄露的测试数据。当前控制器尚未允许部署这一模式。

**策略版本**:
一次不可修改的规则快照。每次更新都产生新版本，并能与日志关联和回滚。

**策略网关**:
位于目标软件和公网之间，透明接管 HTTP/HTTPS，负责核对实际目的 IP、DNS 租约、Host/SNI、端口、协议和正文规则。目标软件没有绕过网关的公网路径；网关故障时 Web 断开。脱离可解析 HTTP/HTTPS 的原始 TCP 和升级协议不转发。

**受控 DNS**:
目标容器唯一的外部名称解析入口。严格和日常策略都接受普通公网域名；危险地址、异常查询类型和解析失败在本地拒绝。查询经上游隧道发出 DoH，校验响应码、名称链和公网地址，先落盘再为成功解析的名称和 IP 签发短期租约。租约 TTL 取完整名称链的最小值，零 TTL 不签租约；同一名称和同一已验证公网 IP 在 TTL 后保留 60 秒连接宽限，以兼容应用 DNS 缓存。策略网关只连接租约中已经批准的 IP，同时保留原域名用于 SNI 和证书校验。

**上游隧道**:
把 DNS 和已经放行的 Web 流量送往公网的 VPN 或代理。DNS 和网关位于无公网路由的内部上游网桥，运行时宿主门禁只允许回环和它们访问父代理端口；停止时和宿主重启恢复后只允许回环。上游可以更换，但不能决定哪些流量获准通过，也不能成为绕过受控 DNS 的解析入口。

**网络 watchdog**:
检查 DNS、网关、三处抓包和目标 cgroup eBPF 是否仍是本次运行登记的进程、是否被暂停或替换。检查通过才续期短期网络放行项；任何必要探针不再满足条件后，放行项自动过期。

## 审计与部署

**基础审计**:
三处 PCAP 保存实际出现的数据包；目标 cgroup eBPF 记录 `execve`、`connect`、`sendto` 和 `sendmsg` 调用；代理保存已经实测支持的 TLS 明文。失败调用可能没有对应数据包。该组合不声称覆盖未列出的内核接口、内核丢包或 eBPF 丢事件。

**针对性明文审计**:
针对一个具体软件设计捕获方法，并用已知测试数据证明它发出的指定内容确实可见。通用网关不为了保存响应正文而延迟转发；确需分析响应正文时使用独立的专项捕获。

**待查通信**:
无法说明来源、目的或内容可见范围的通信。进入日常策略前存在待查通信时不能放行；日常运行中新出现的待查通信若无法及时解释，应切回严格策略。

**对比运行**:
用相同软件、任务和出口，只改变一种检测条件的重复测试。它用于判断检测本身是否改变了目标软件的行为。

**主机配置**:
一台服务器自己的项目路径、开发环境、存储和上游配置。路径尽量相对于用户 Home 记录。

**发布版本**:
用于多服务器部署的一组固定控制代码、镜像、策略和文档。本地项目、开发环境、数据、凭据和普通日志不随它发布。
