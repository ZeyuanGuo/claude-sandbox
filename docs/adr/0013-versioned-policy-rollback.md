# 每次策略更新必须留档并可完整回滚

每次增加、修改或删除规则都产生新版本，记录差异、理由、证据、操作者和时间。只有校验通过的版本才能应用；状态和审计记录必须写明当时使用的版本。

发现异常或泄露后，可以一次恢复到上一份已验证策略，不能留下新旧规则混用的中间状态。上游凭据、主机路径和运行时秘密不进入策略版本，也不随策略回滚。

首次脱敏发布重新建立一条不含主机私有端点和现场标识的公开策略谱系。未发布的本机历史快照不作为公开父链；从首次发布提交开始，公开快照继续遵守只新增、不修改。
